# Francês (fr-FR) como quinto idioma — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar francês (fr-FR) com paridade total aos outros quatro idiomas, protegido por um guard automatizado que impede shipar um locale incompleto.

**Architecture:** Três testes Vitest primeiro, escritos por locale e validados contra o estado completo atual (verde com 4 locales). Só então `fr` entra em `LOCALES`, o que vira os testes vermelhos e transforma a saída deles no checklist de tradução. Cada task seguinte derruba uma fatia do contador até zerar.

**Tech Stack:** Next.js 16 (App Router), TypeScript 5.9.3, Vitest 4.1.5, `typescript` como API de parsing (AST), pnpm, Node 24 via nvm.

## Global Constraints

- `nvm use 24` antes de qualquer comando `pnpm` — pnpm só existe sob Node 24.
- Branch: `feat/i18n-french` (já criada a partir da `main` atualizada). Não commitar na `main`.
- Variante **fr-FR** (França). Vocabulário europeu: `e-mail`, não `courriel`.
- **Termos técnicos permanecem em inglês**: React, Next.js, TypeScript, cursor-based, multi-tenant, Row Level Security, dead letter queue, marketplace, full-stack, performance engineering.
- **Números e métricas preservados exatamente.** Separador decimal vírgula (`6,7 MB`). Unidades ficam `MB`/`KB`, nunca `Mo`/`Ko`.
- **Tradução é tradução.** Nenhuma afirmação, métrica, empresa ou data nova. Blocos `type: "code"` dentro dos posts não são traduzidos — apenas o texto ao redor.
- Tipografia francesa: espaço fino antes de `:`, `;`, `?`, `!` (ex.: `atout : paiements`).
- Mensagens de commit em português, sem acentuação no corpo, terminando com:
  `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`
- Não abrir PR até a Task 12. O CI roda uma vez, no fim.

## Mapa de arquivos

**Criados:**
- `lib/i18n-coverage.test.ts` — os três testes do guard. Única responsabilidade: falhar enquanto algum locale estiver incompleto.

**Modificados (estrutura):**
- `lib/i18n.ts` — `LOCALES`, mapas exaustivos `HTML_LANGS`/`OG_LOCALES`, `localeToOgLocale()`
- `proxy.ts` — passa a importar de `@/lib/i18n` em vez de duplicar a lista
- `app/[locale]/layout.tsx` — consome `localeToOgLocale()`
- `lib/posts.ts` — sufixo derivado do locale, campos `_fr` em `RawPostData`
- `lib/i18n-server.ts` — `const fr: Dictionary` + branch em `getDictionary`
- `components/language-toggle.tsx` — `flags` e `labels`

**Modificados (conteúdo):** 16 arquivos com objetos de tradução + 11 JSONs de post + `public/llms.txt`, `README.md`, `CLAUDE.md`, `data/experiences.test.ts`.

## Contador de referência

Rodei o parser AST contra o código atual simulando `fr` já em `LOCALES`. **35 arquivos varridos, 281 objetos incompletos, zero falsos positivos** (todo objeto sinalizado falta exatamente `fr`, nada mais). Distribuição verificada — é o mapa de progresso das tasks 3 a 8:

| Arquivo | Objetos | Task |
|---|---|---|
| `app/[locale]/engenharia/page.tsx` | 94 | 3 |
| `data/experiences.ts` | 52 | 8 |
| `app/[locale]/uses/page.tsx` | 48 | 4 |
| `app/[locale]/sobre/page.tsx` | 25 | 5 |
| `data/projects.ts` | 14 | 7 |
| `app/[locale]/busca/page.tsx` | 9 | 6 |
| `app/[locale]/page.tsx` | 8 | 6 |
| `app/[locale]/not-found.tsx` | 5 | 6 |
| `app/[locale]/projetos/page.tsx` | 5 | 6 |
| `components/footer.tsx` | 4 | 6 |
| `app/[locale]/layout.tsx` | 3 | 6 |
| `app/[locale]/projetos/ikropp/page.tsx` | 3 | 6 |
| `app/[locale]/projetos/vox-pet-digital/page.tsx` | 3 | 6 |
| `app/[locale]/projetos/x-drop/page.tsx` | 3 | 6 |
| `components/experience-item.tsx` | 3 | 6 |
| `components/language-toggle.tsx` | 2 | 2 |

A spec previa 8 commits; este plano usa 12 tasks. A diferença é só granularidade — a spec agrupava "strings `t()` das páginas" num commit, e aqui isso vira quatro tasks (3 a 6) porque os arquivos têm tamanhos muito diferentes e um revisor pode aceitar a tradução de um e rejeitar a de outro. A ordem e os marcos são os mesmos.

---

### Task 1: Guard de cobertura (verde com 4 locales)

Escreve os três testes e prova que passam **antes** de `fr` existir. Isso valida que a regra não tem falso positivo e que o estado atual é de fato completo — se algum teste falhar aqui, o bug é no teste, não no conteúdo.

**Files:**
- Create: `lib/i18n-coverage.test.ts`

**Interfaces:**
- Consumes: `LOCALES`, `DEFAULT_LOCALE` de `@/lib/i18n`; `getDictionary` de `@/lib/i18n-server`
- Produces: nada importável. Tasks 2–11 dependem da **saída** deste teste como checklist.

- [ ] **Step 1: Escrever o teste**

Criar `lib/i18n-coverage.test.ts`:

```ts
import fs from "node:fs"
import path from "node:path"
import ts from "typescript"
import { describe, it, expect } from "vitest"
import { LOCALES, DEFAULT_LOCALE } from "@/lib/i18n"
import { getDictionary } from "@/lib/i18n-server"

// process.cwd() é a raiz do repo sob Vitest — mesmo padrão que lib/posts.ts já usa
const ROOT = process.cwd()

const SCAN_DIRS = ["app", "components"]
const SCAN_FILES = ["data/projects.ts", "data/experiences.ts"]
const SKIPPED_DIR = path.join("components", "ui") // código gerado do Shadcn

const POST_STRING_FIELDS = ["title", "description", "date", "readTime", "tag"] as const

function collectFiles(dir: string): string[] {
  const out: string[] = []
  for (const entry of fs.readdirSync(path.join(ROOT, dir), { withFileTypes: true })) {
    const rel = path.join(dir, entry.name)
    if (entry.isDirectory()) {
      if (rel === SKIPPED_DIR) continue
      out.push(...collectFiles(rel))
      continue
    }
    if (!/\.tsx?$/.test(entry.name)) continue
    if (entry.name.includes(".test.")) continue
    out.push(rel)
  }
  return out
}

function keyOf(prop: ts.ObjectLiteralElementLike): string | null {
  if (!ts.isPropertyAssignment(prop) && !ts.isShorthandPropertyAssignment(prop)) return null
  const { name } = prop
  if (ts.isIdentifier(name) || ts.isStringLiteral(name)) return name.text
  return null
}

function findGaps(relPath: string): string[] {
  const source = fs.readFileSync(path.join(ROOT, relPath), "utf-8")
  // ScriptKind por extensão: TSX num .ts faz o parser confundir generics com JSX
  const kind = relPath.endsWith(".tsx") ? ts.ScriptKind.TSX : ts.ScriptKind.TS
  const sf = ts.createSourceFile(relPath, source, ts.ScriptTarget.Latest, true, kind)
  const gaps: string[] = []

  function visit(node: ts.Node) {
    if (ts.isObjectLiteralExpression(node)) {
      const keys = new Set(node.properties.map(keyOf).filter((k): k is string => k !== null))
      if (keys.has(DEFAULT_LOCALE)) {
        const missing = LOCALES.filter((l) => !keys.has(l))
        if (missing.length > 0) {
          const { line } = sf.getLineAndCharacterOfPosition(node.getStart(sf))
          gaps.push(`${relPath}:${line + 1} → falta ${missing.join(", ")}`)
        }
      }
    }
    ts.forEachChild(node, visit)
  }

  visit(sf)
  return gaps
}

describe("cobertura de i18n", () => {
  it("traduz todo object literal com chave pt para todos os locales", () => {
    const files = [...SCAN_DIRS.flatMap(collectFiles), ...SCAN_FILES]
    const gaps = files.flatMap(findGaps)
    // console.error em vez de mensagem de assert: o Vitest trunca diffs grandes,
    // e essa lista e o checklist de traducao -- precisa sair inteira
    if (gaps.length > 0) {
      console.error(`\n${gaps.length} objeto(s) incompleto(s):\n${gaps.join("\n")}\n`)
    }
    expect(gaps.length, "objeto(s) incompleto(s) — lista acima").toBe(0)
  })

  it("retorna um Dictionary distinto para cada locale", () => {
    const base = getDictionary(DEFAULT_LOCALE)
    for (const locale of LOCALES) {
      if (locale === DEFAULT_LOCALE) continue
      expect(
        getDictionary(locale),
        `getDictionary("${locale}") caiu no dicionário padrão`,
      ).not.toBe(base)
    }
  })

  it("traduz todo post para todos os locales", () => {
    const dir = path.join(ROOT, "data", "posts")
    const files = fs.readdirSync(dir).filter((f) => f.endsWith(".json") && !f.startsWith("."))
    const gaps: string[] = []

    for (const file of files) {
      const raw = JSON.parse(fs.readFileSync(path.join(dir, file), "utf-8")) as Record<string, unknown>
      for (const locale of LOCALES) {
        if (locale === DEFAULT_LOCALE) continue
        for (const field of POST_STRING_FIELDS) {
          const value = raw[`${field}_${locale}`]
          if (typeof value !== "string" || value.trim() === "") {
            gaps.push(`${file} → ${field}_${locale}`)
          }
        }
        const blocks = raw[`blocks_${locale}`]
        if (!Array.isArray(blocks) || blocks.length === 0) {
          gaps.push(`${file} → blocks_${locale}`)
        }
      }
    }

    if (gaps.length > 0) {
      console.error(`\n${gaps.length} campo(s) faltando:\n${gaps.join("\n")}\n`)
    }
    expect(gaps.length, "campo(s) de post faltando — lista acima").toBe(0)
  })
})
```

- [ ] **Step 2: Rodar e verificar que PASSA**

```bash
nvm use 24 && pnpm vitest run lib/i18n-coverage.test.ts
```

Esperado: **3 passed**. Os quatro locales atuais já têm cobertura total — se algum falhar, o parser está com bug (ou achou um gap real em `jp`/`es`, que também é informação boa).

- [ ] **Step 3: Verificar que a suíte inteira segue verde**

```bash
pnpm test
```

Esperado: todos os arquivos de teste passam.

- [ ] **Step 4: Commit**

```bash
git add lib/i18n-coverage.test.ts
git commit -m "$(cat <<'EOF'
test(i18n): guard de cobertura por locale

Tres testes que falham quando um locale fica incompleto: object
literals com chave pt precisam cobrir todo LOCALES (via AST do
TypeScript), getDictionary precisa retornar objeto distinto por
locale, e todo post precisa dos 6 campos _<locale>.

Escritos por locale, nao para um idioma especifico. Verdes com os
quatro atuais -- confirma que pt/en/es/jp estao completos e que a
regra nao gera falso positivo.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: Estrutura do locale `fr`

Adiciona `fr` e fecha os lugares que repetem a lista de locales à mão. No fim desta task, `typecheck` e `build` passam, e o guard fica **vermelho com 281 gaps** — que é o comportamento desejado e o checklist das próximas tasks.

**Files:**
- Modify: `lib/i18n.ts:1`, `lib/i18n.ts:27-33`
- Modify: `proxy.ts:1-9`
- Modify: `app/[locale]/layout.tsx:53`, `app/[locale]/layout.tsx:66-68`
- Modify: `lib/posts.ts:1-4`, `lib/posts.ts:200-229`
- Modify: `lib/i18n-server.ts:211-218`
- Modify: `components/language-toggle.tsx:13-14`

**Interfaces:**
- Produces: `localeToOgLocale(locale: Locale): string` exportada de `@/lib/i18n` — usada por `app/[locale]/layout.tsx`. `localeToHtmlLang` mantém a assinatura `(locale: Locale) => string`.
- Consumes: nada de tasks anteriores.

- [ ] **Step 1: Adicionar `fr` e trocar as cadeias de ternário por mapas exaustivos**

Em `lib/i18n.ts`, linha 1:

```ts
export const LOCALES = ["pt", "en", "es", "jp", "fr"] as const
```

Substituir a função `localeToHtmlLang` (linhas 27-33) por:

```ts
/** HTML lang attribute mapping (more specific than the locale code) */
const HTML_LANGS: Record<Locale, string> = {
  pt: "pt-BR",
  en: "en-US",
  es: "es-ES",
  jp: "ja-JP",
  fr: "fr-FR",
}

export function localeToHtmlLang(locale: Locale): string {
  return HTML_LANGS[locale]
}

/** Open Graph locale mapping */
const OG_LOCALES: Record<Locale, string> = {
  pt: "pt_BR",
  en: "en_US",
  es: "es_ES",
  jp: "ja_JP",
  fr: "fr_FR",
}

export function localeToOgLocale(locale: Locale): string {
  return OG_LOCALES[locale]
}
```

O `Record<Locale, string>` é o ponto: o `tsc` passa a exigir toda entrada. A versão antiga era uma cadeia de `if` que caía em `pt-BR` para qualquer locale não previsto — com `fr` adicionado e essa função intocada, as páginas francesas sairiam anunciando `lang="pt-BR"`.

- [ ] **Step 2: Eliminar a lista duplicada do proxy**

Substituir `proxy.ts` inteiro por:

```ts
import { NextResponse, type NextRequest } from "next/server"
import { DEFAULT_LOCALE, isLocale } from "@/lib/i18n"

export function proxy(request: NextRequest) {
  const { pathname } = request.nextUrl
  const segments = pathname.split("/").filter(Boolean)
  const first = segments[0]

  if (isLocale(first)) {
    const headers = new Headers(request.headers)
    headers.set("x-locale", first)
    headers.set("x-pathname", pathname)
    return NextResponse.next({ request: { headers } })
  }

  const url = request.nextUrl.clone()
  url.pathname = `/${DEFAULT_LOCALE}${pathname === "/" ? "" : pathname}`
  return NextResponse.redirect(url, 308)
}

export const config = {
  matcher: ["/((?!_next|api|.*\\..*).*)"],
}
```

Import seguro: `lib/i18n.ts` não tem nenhum import próprio (só constantes e funções puras) e o alias `@/*` resolve da raiz via `tsconfig.json`.

- [ ] **Step 3: Consumir `localeToOgLocale` no layout**

Em `app/[locale]/layout.tsx`, adicionar `localeToOgLocale` ao import de `@/lib/i18n` (junto de `LOCALES`, `isLocale`, …). Trocar a linha 53:

```ts
const ogLocale = localeToOgLocale(locale)
```

E o bloco `alternateLocale` (linhas 66-68):

```ts
      alternateLocale: LOCALES.filter((l) => l !== locale).map(localeToOgLocale),
```

- [ ] **Step 4: Generalizar o sufixo de locale nos posts**

Em `lib/posts.ts`, trocar o import da linha 4:

```ts
import { DEFAULT_LOCALE, type Locale } from "@/lib/i18n"
```

(Importar de `@/lib/i18n` e não de `@/lib/i18n-server` — este último puxa `next/headers`, que não deve entrar em `posts.ts` porque `app/sitemap.ts` a consome.)

Trocar a linha 229:

```ts
  const suffix = locale === DEFAULT_LOCALE ? null : `_${locale}`
```

E adicionar os campos `_fr` ao `RawPostData` (após o bloco `_jp`):

```ts
  title_fr?: string
  description_fr?: string
  content_fr?: string
  blocks_fr?: unknown[]
  date_fr?: string
  readTime_fr?: string
  tag_fr?: string
```

- [ ] **Step 5: Dicionário francês**

Em `lib/i18n-server.ts`, adicionar depois de `const jp` (linha 211):

```ts
const fr: Dictionary = {
  nav: {
    home: "Accueil",
    about: "À propos",
    projects: "Projets",
    engineering: "Ingénierie",
    uses: "Outils",
    search: "Recherche",
    curriculum: "CV",
  },

  home: {
    title: "Vinicius Aguiar",

    subtitle:
      "Frontend Engineer qui construit des interfaces rapides et scalables pour des SaaS en production — React, Next.js et performance engineering.",

    description:
      "TypeScript de bout en bout. Une polyvalence full-stack comme atout : paiements, architecture multi-tenant et IA en production.",

    ctaAbout: "À propos",
    ctaResume: "Télécharger le CV",
    ctaContact: "Me contacter",
  },

  about: {
    title: "À propos",
    subtitle: "Un aperçu de mon parcours, de mes compétences et de mes centres d'intérêt.",
  },

  projects: {
    title: "Projets",
    subtitle:
      "Entreprises et produits sur lesquels j'ai travaillé — SaaS, marketplaces, ERP et plateformes digitales.",
  },

  ui: {
    search: "Rechercher",
  },
}
```

E o branch em `getDictionary`:

```ts
export function getDictionary(locale: Locale): Dictionary {
  if (locale === "en") return en
  if (locale === "es") return es
  if (locale === "jp") return jp
  if (locale === "fr") return fr
  return pt
}
```

- [ ] **Step 6: Bandeira e rótulo no toggle**

Em `components/language-toggle.tsx`, linhas 13-14:

```ts
const flags: Record<Locale, string> = { pt: "🇧🇷", en: "🇺🇸", es: "🇪🇸", jp: "🇯🇵", fr: "🇫🇷" }
const labels: Record<Locale, string> = { pt: "Português", en: "English", es: "Español", jp: "日本語", fr: "Français" }
```

- [ ] **Step 7: Verificar que typecheck e build passam**

```bash
nvm use 24 && pnpm typecheck && pnpm lint
```

Esperado: ambos limpos. Se o `tsc` reclamar de algum `Record<Locale, string>` fora dos arquivos acima, é um mapa exaustivo que eu não mapeei — adicionar `fr` nele e anotar.

- [ ] **Step 8: Verificar que o guard ficou vermelho com o número esperado**

```bash
pnpm vitest run lib/i18n-coverage.test.ts
```

Esperado: **2 failed, 1 passed**.
- "traduz todo object literal…" → falha com **279 objeto(s) incompleto(s)**
- "traduz todo post…" → falha com **66 campo(s) faltando** (11 posts × 6 campos)
- "retorna um Dictionary distinto…" → **passa** (o dicionário `fr` foi criado no Step 5)

São 279 e não 281 porque o Step 6 desta mesma task fecha os 2 objetos de `components/language-toggle.tsx` (`flags` e `labels`). O total de 281 da tabela do contador é `2` (esta task) + `279` (tasks 3 a 8) — e a coluna de tasks daquela tabela soma exatamente 279.

Se o número não for exatamente 279, algo divergiu do estado medido — investigar antes de seguir.

- [ ] **Step 9: Verificar que o build gera as rotas francesas**

```bash
pnpm build
```

Esperado: build verde, com rotas `/fr/...` na saída. O conteúdo ainda sai em português — é o esperado nesta task.

- [ ] **Step 10: Commit**

```bash
git add lib/i18n.ts proxy.ts app/\[locale\]/layout.tsx lib/posts.ts lib/i18n-server.ts components/language-toggle.tsx
git commit -m "$(cat <<'EOF'
feat(i18n): adiciona locale fr e centraliza a lista de idiomas

LOCALES passa a ser a unica fonte de verdade: proxy.ts importa em vez
de duplicar a lista, e as duas cadeias de ternario que mapeavam locale
para lang/og viram Record<Locale,string> exaustivo -- o tsc passa a
exigir toda entrada em vez de cair em pt-BR calado.

Sufixo dos posts derivado do locale, dicionario fr, bandeira no toggle.
Guard fica vermelho com 281 objetos e 66 campos de post pendentes: e o
checklist das proximas tasks.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: Traduzir `engenharia/page.tsx` (94 objetos)

Maior arquivo de strings do site. Contém a FAQ que alimenta o schema `FAQPage` do AEO.

**Files:**
- Modify: `app/[locale]/engenharia/page.tsx`

**Interfaces:**
- Consumes: `LOCALES` já contém `fr` (Task 2)
- Produces: nada importável

- [ ] **Step 1: Listar os objetos pendentes deste arquivo**

```bash
nvm use 24 && pnpm vitest run lib/i18n-coverage.test.ts 2>&1 | grep "engenharia/page.tsx"
```

Esperado: 94 linhas, cada uma `app/[locale]/engenharia/page.tsx:N → falta fr`.

- [ ] **Step 2: Adicionar a chave `fr` em cada objeto**

Para cada objeto sinalizado, acrescentar `fr:` como última chave, traduzindo a partir do `pt:` e consultando `en:` para consistência de terminologia. Padrão:

```ts
// antes
{ pt: "Como eu penso sobre sistemas", en: "How I think about systems", es: "Cómo pienso sobre sistemas", jp: "システムをどう設計するか" }

// depois
{ pt: "Como eu penso sobre sistemas", en: "How I think about systems", es: "Cómo pienso sobre sistemas", jp: "システムをどう設計するか", fr: "Ma façon de penser les systèmes" }
```

Aplicar as Global Constraints — termos técnicos em inglês, métricas idênticas, espaço antes de `:` e `?`. Exemplo com métrica:

```ts
{ pt: "payload de 6,7 MB para 19 KB (~400x)", ..., fr: "payload de 6,7 MB à 19 KB (~400x)" }
```

- [ ] **Step 3: Verificar que o arquivo zerou**

```bash
pnpm vitest run lib/i18n-coverage.test.ts 2>&1 | grep -c "engenharia/page.tsx"
```

Esperado: `0`.

- [ ] **Step 4: Verificar typecheck**

```bash
pnpm typecheck
```

Esperado: limpo.

- [ ] **Step 5: Commit**

```bash
git add "app/[locale]/engenharia/page.tsx"
git commit -m "$(cat <<'EOF'
feat(i18n): traduz a pagina Engenharia para frances

94 objetos, incluindo a FAQ que alimenta o schema FAQPage.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: Traduzir `uses/page.tsx` (48 objetos)

**Files:**
- Modify: `app/[locale]/uses/page.tsx`

- [ ] **Step 1: Listar os objetos pendentes**

```bash
nvm use 24 && pnpm vitest run lib/i18n-coverage.test.ts 2>&1 | grep "uses/page.tsx"
```

Esperado: 48 linhas.

- [ ] **Step 2: Adicionar a chave `fr` em cada objeto**

Aqui os objetos são majoritariamente `{ pt, en, es, jp }` inline em `title` e `description` de ferramentas — não passam por `t()`, mas o guard os cobre igual. Padrão:

```ts
// antes
title: { pt: "Editor e Coding", en: "Editor & Coding", es: "Editor y Coding", jp: "エディタとコーディング" },

// depois
title: { pt: "Editor e Coding", en: "Editor & Coding", es: "Editor y Coding", jp: "エディタとコーディング", fr: "Éditeur et Coding" },
```

Nomes de produtos (VS Code, Warp, Vercel, Docker) não são traduzidos — apenas as descrições ao redor.

- [ ] **Step 3: Verificar que o arquivo zerou**

```bash
pnpm vitest run lib/i18n-coverage.test.ts 2>&1 | grep -c "uses/page.tsx"
```

Esperado: `0`.

- [ ] **Step 4: Verificar typecheck**

```bash
pnpm typecheck
```

Esperado: limpo.

- [ ] **Step 5: Commit**

```bash
git add "app/[locale]/uses/page.tsx"
git commit -m "$(cat <<'EOF'
feat(i18n): traduz a pagina Ferramentas para frances

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: Traduzir `sobre/page.tsx` (25 objetos)

**Files:**
- Modify: `app/[locale]/sobre/page.tsx`

- [ ] **Step 1: Listar os objetos pendentes**

```bash
nvm use 24 && pnpm vitest run lib/i18n-coverage.test.ts 2>&1 | grep "sobre/page.tsx"
```

Esperado: 25 linhas.

- [ ] **Step 2: Adicionar a chave `fr` em cada objeto**

Para cada objeto sinalizado, acrescentar `fr:` como última chave, traduzindo a partir do `pt:` e consultando `en:` para consistência de terminologia:

```ts
// antes
{ pt: "Sobre mim", en: "About me", es: "Sobre mí", jp: "プロフィール" }

// depois
{ pt: "Sobre mim", en: "About me", es: "Sobre mí", jp: "プロフィール", fr: "À propos de moi" }
```

Aplicar as Global Constraints — termos técnicos em inglês, métricas idênticas com vírgula decimal, espaço antes de `:` e `?`.

Atenção aos rótulos da timeline de experiência, que precisam bater exatamente com os termos usados em `components/experience-item.tsx` (Task 6): o rótulo de posição atual é `fr: "Actuel"`.

- [ ] **Step 3: Verificar que o arquivo zerou**

```bash
pnpm vitest run lib/i18n-coverage.test.ts 2>&1 | grep -c "sobre/page.tsx"
```

Esperado: `0`.

- [ ] **Step 4: Verificar typecheck**

```bash
pnpm typecheck
```

Esperado: limpo.

- [ ] **Step 5: Commit**

```bash
git add "app/[locale]/sobre/page.tsx"
git commit -m "$(cat <<'EOF'
feat(i18n): traduz a pagina Sobre para frances

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: Traduzir páginas restantes e componentes (46 objetos)

**Files:**
- Modify: `app/[locale]/busca/page.tsx` (9), `app/[locale]/page.tsx` (8), `app/[locale]/not-found.tsx` (5), `app/[locale]/projetos/page.tsx` (5), `app/[locale]/layout.tsx` (3), `app/[locale]/projetos/ikropp/page.tsx` (3), `app/[locale]/projetos/vox-pet-digital/page.tsx` (3), `app/[locale]/projetos/x-drop/page.tsx` (3)
- Modify: `components/footer.tsx` (4), `components/experience-item.tsx` (3)
- Modify: `components/projects-grid.tsx` (0 objetos — ver Step 3)

As três páginas de detalhe de projeto são exatamente as que foram esquecidas na adição do japonês (`89d919f`) — o guard agora as força.

**Ponto cego do guard, descoberto na Task 3.** O guard só enxerga object literals com chave `pt`. Cadeias ternárias `locale === "en" ? … : locale === "es" ? … : "português"` são invisíveis para ele: caem no fallback português e o contador marca zero. Três strings estão nessa forma e precisam virar chamadas `t()` — o que as torna visíveis ao guard permanentemente. `components/projects-grid.tsx` não aparecia no plano por esse motivo: não tem nenhum objeto com `pt:`.

- [ ] **Step 1: Listar todos os objetos pendentes**

```bash
nvm use 24 && pnpm vitest run lib/i18n-coverage.test.ts 2>&1 | grep "→ falta"
```

Esperado: 46 linhas, distribuídas nos 10 arquivos acima.

- [ ] **Step 2: Adicionar a chave `fr` em cada objeto**

Para cada objeto sinalizado, acrescentar `fr:` como última chave, traduzindo a partir do `pt:` e consultando `en:`:

```ts
// antes
{ pt: "Voltar ao início", en: "Back to home", es: "Volver al inicio", jp: "ホームに戻る" }

// depois
{ pt: "Voltar ao início", en: "Back to home", es: "Volver al inicio", jp: "ホームに戻る", fr: "Retour à l'accueil" }
```

Aplicar as Global Constraints — termos técnicos em inglês, métricas idênticas com vírgula decimal, espaço antes de `:` e `?`. Pontos de atenção:

`app/[locale]/layout.tsx` — os 3 objetos são `title`, `description` e `twitter.description` do metadata. O título segue o padrão dos outros idiomas, que mantêm o cargo em inglês:

```ts
fr: "Vinicius Aguiar — Frontend Engineer | React, Next.js & Performance Engineering"
```

`components/experience-item.tsx` — o rótulo "Atual" precisa ser idêntico ao usado em `sobre/page.tsx` (Task 5): `fr: "Actuel"`. Os rótulos do toggle: `fr: "Lire plus"` / `fr: "Lire moins"`.

- [ ] **Step 3: Converter as três cadeias ternárias em chamadas `t()`**

Estas não aparecem na lista do Step 1 — o guard não as enxerga. Sem esta conversão, saem em português no site francês com o contador marcando zero.

`components/projects-grid.tsx:163`:

```tsx
// antes
{locale === "en" ? "Learn more" : locale === "es" ? "Ver más" : locale === "jp" ? "詳しく見る" : "Ver mais"}

// depois
{t(locale, { pt: "Ver mais", en: "Learn more", es: "Ver más", jp: "詳しく見る", fr: "En savoir plus" })}
```

`components/projects-grid.tsx:189`:

```tsx
// antes
{locale === "en" ? "Visit website" : locale === "es" ? "Visitar sitio" : locale === "jp" ? "サイトを開く" : "Acessar site"}

// depois
{t(locale, { pt: "Acessar site", en: "Visit website", es: "Visitar sitio", jp: "サイトを開く", fr: "Visiter le site" })}
```

`app/[locale]/projetos/x-drop/page.tsx:348`:

```tsx
// antes
{locale === "en" ? "iOS App — Swift UI (native)" : locale === "es" ? "App iOS — Swift UI (nativo)" : locale === "jp" ? "iOSアプリ — Swift UI（ネイティブ）" : "App iOS — Swift UI (nativo)"}

// depois
{t(locale, { pt: "App iOS — Swift UI (nativo)", en: "iOS App — Swift UI (native)", es: "App iOS — Swift UI (nativo)", jp: "iOSアプリ — Swift UI（ネイティブ）", fr: "App iOS — Swift UI (natif)" })}
```

Adicionar o import de `t` em `components/projects-grid.tsx` se ainda não existir. Depois da conversão, esses três passam a contar no guard — o Step 4 já considera isso.

A cadeia ternária em `lib/i18n-server.ts:255-258` (`getDictionary`) **não** é convertida: ela seleciona dicionários inteiros, não strings, e já está coberta pelo Teste 2 do guard.

- [ ] **Step 4: Verificar que só os arquivos de dados seguem pendentes**

```bash
pnpm vitest run lib/i18n-coverage.test.ts 2>&1 | grep "objeto(s) incompleto"
```

Esperado: `66 objeto(s) incompleto(s)` — os 14 de `data/projects.ts` (Task 7) e os 52 de `data/experiences.ts` (Task 8). Nenhum arquivo sob `app/` ou `components/` deve aparecer mais na lista.

(Não confundir com os outros 66 do Teste 3, que são campos de post — 11 posts × 6 campos. Contadores diferentes, testes diferentes.)

- [ ] **Step 5: Verificar typecheck**

```bash
pnpm typecheck
```

Esperado: limpo.

- [ ] **Step 6: Commit**

```bash
git add "app/[locale]" components/footer.tsx components/experience-item.tsx components/projects-grid.tsx
git commit -m "$(cat <<'EOF'
feat(i18n): traduz paginas restantes e componentes para frances

Home, busca, 404, projetos, layout, as tres paginas de detalhe de
projeto, footer e experience-item. As paginas de projeto foram
exatamente as esquecidas na adicao do japones (89d919f) -- agora o
guard as obriga.

Converte tres cadeias ternarias por locale em chamadas t() (dois
labels em projects-grid, um em x-drop). Elas eram invisiveis ao guard
e cairiam no fallback portugues; como t(), passam a ser cobertas.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: Traduzir `data/projects.ts` (14 objetos)

7 projetos × `tagline` + `description`.

**Files:**
- Modify: `data/projects.ts`

- [ ] **Step 1: Listar os objetos pendentes**

```bash
nvm use 24 && pnpm vitest run lib/i18n-coverage.test.ts 2>&1 | grep "projects.ts"
```

Esperado: 14 linhas.

- [ ] **Step 2: Adicionar `fr` em cada `tagline` e `description`**

O tipo é `LocalizedString = Partial<Record<Locale, string>> & { pt: string }` — `fr` é opcional pelo tipo, obrigatório pelo guard. Padrão:

```ts
    tagline: {
      pt: "Outreach e customer engagement com IA — 100+ empresas",
      en: "AI-powered outreach and customer engagement — 100+ companies",
      es: "Outreach y customer engagement con IA — 100+ empresas",
      jp: "AIによるアウトリーチ＆カスタマーエンゲージメント — 100社以上",
      fr: "Outreach et customer engagement avec IA — 100+ entreprises",
    },
```

Nas `description`, preservar as métricas exatas (`~6,7 MB`, `~19 KB`, `R$ 30k+`) e a lista de stack sem tradução.

- [ ] **Step 3: Verificar que o arquivo zerou**

```bash
pnpm vitest run lib/i18n-coverage.test.ts 2>&1 | grep -c "projects.ts"
```

Esperado: `0`.

- [ ] **Step 4: Verificar typecheck**

```bash
pnpm typecheck
```

Esperado: limpo.

- [ ] **Step 5: Commit**

```bash
git add data/projects.ts
git commit -m "$(cat <<'EOF'
feat(i18n): traduz taglines e descricoes dos projetos para frances

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 8: Traduzir `data/experiences.ts` (52 objetos) e fechar o Teste 1

Esta task muda o **tipo** `Localized` para incluir `fr`, o que torna a tradução obrigatória em compile-time, e conserta uma lista de locales hardcoded que sobrou no arquivo de teste.

**Files:**
- Modify: `data/experiences.ts:1`
- Modify: `data/experiences.test.ts:2-4`, `data/experiences.test.ts:26`

**Interfaces:**
- Produces: `Localized = { pt: string; en: string; es: string; jp: string; fr: string }` — consumido por `components/experience-item.tsx` via `t()`.

- [ ] **Step 1: Estreitar o tipo para forçar o francês**

Em `data/experiences.ts`, linha 1:

```ts
export type Localized = { pt: string; en: string; es: string; jp: string; fr: string }
```

- [ ] **Step 2: Rodar o typecheck para gerar o checklist**

```bash
nvm use 24 && pnpm typecheck
```

Esperado: **falha**, listando cada objeto `Localized` sem `fr`. Diferente das tasks anteriores, aqui o compilador é quem cobra — `Localized` é exaustivo por design.

- [ ] **Step 3: Adicionar `fr` em todos os campos**

`role`, `period`, `summary`, `highlight` e cada item de `details`, nas 6 empresas. Padrões:

```ts
    role: { pt: "Product Engineer (Frontend)", en: "Product Engineer (Frontend)", es: "Product Engineer (Frontend)", jp: "プロダクトエンジニア（フロントエンド）", fr: "Product Engineer (Frontend)" },
```

```ts
    period: {
      pt: "Jul 2026 — atual · Tempo integral · Remoto",
      en: "Jul 2026 — present · Full-time · Remote",
      es: "Jul 2026 — presente · Tiempo completo · Remoto",
      jp: "2026年7月 — 現在 · フルタイム · リモート",
      fr: "Juil. 2026 — aujourd'hui · Temps plein · À distance",
    },
```

```ts
    highlight: {
      pt: "Inbox: payload ~6,7 MB → ~19 KB (~400x)",
      ...
      fr: "Inbox : payload ~6,7 MB → ~19 KB (~400x)",
    },
```

Vocabulário de regime, consistente nas 6 empresas: `Temps plein`, `Temps partiel`, `Contrat`, `Hybride`, `À distance`.

- [ ] **Step 4: Consertar a lista hardcoded no teste de experiências**

`data/experiences.test.ts:4` declara a própria lista de locales, que não acompanha `LOCALES` — é a mesma classe de bug que causou `0c415ab`. Trocar as linhas 1-4 por:

```ts
import { describe, it, expect } from "vitest"
import { experiences } from "./experiences"
import { LOCALES } from "@/lib/i18n"
```

(remover o `const LOCALES` local) e a linha 26 por:

```ts
    const contractWord = { pt: "Contrato", en: "Contract", es: "Contrato", jp: "業務委託", fr: "Contrat" }
```

Com isso o teste "fills every localized field in all four locales" passa a cobrir os cinco automaticamente. Renomear o título do teste para `"fills every localized field in every locale"`.

- [ ] **Step 5: Verificar typecheck e o Teste 1 do guard**

```bash
pnpm typecheck && pnpm vitest run lib/i18n-coverage.test.ts
```

Esperado: typecheck limpo. No guard, **1 failed, 2 passed** — só "traduz todo post…" segue vermelho, com 66 campos. O Teste 1 (object literals) está verde: os 281 objetos zeraram.

- [ ] **Step 6: Rodar a suíte inteira**

```bash
pnpm test
```

Esperado: `experiences.test.ts` verde nos cinco locales.

- [ ] **Step 7: Commit**

```bash
git add data/experiences.ts data/experiences.test.ts
git commit -m "$(cat <<'EOF'
feat(i18n): traduz experiencias para frances e zera o guard de objetos

Localized passa a exigir fr, entao o tsc cobra os 52 campos em vez do
guard. experiences.test.ts tinha a propria lista de locales hardcoded
-- mesma classe de bug do 0c415ab; agora importa LOCALES e cobre os
cinco idiomas automaticamente.

Teste 1 do guard verde: 281 objetos zerados.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 9: Traduzir posts 1–4

Os 11 posts saem em três tasks para manter cada diff revisável. Cada post precisa de 6 campos: `title_fr`, `description_fr`, `date_fr`, `readTime_fr`, `tag_fr`, `blocks_fr`.

**Files:**
- Modify: `data/posts/case-study-vox-pet.json`, `data/posts/case-study-xdrop.json`, `data/posts/circuit-breaker-nodejs.json`, `data/posts/design-system-shadcn-tailwind.json`

- [ ] **Step 1: Confirmar os campos pendentes**

```bash
nvm use 24 && pnpm vitest run lib/i18n-coverage.test.ts 2>&1 | grep -E "case-study-vox-pet|case-study-xdrop|circuit-breaker|design-system"
```

Esperado: 24 linhas (4 posts × 6 campos).

- [ ] **Step 2: Traduzir os quatro posts**

Para cada arquivo, adicionar os 6 campos espelhando a estrutura de `_jp`. Formatos dos campos curtos:

| Campo | `pt` | `fr` |
|---|---|---|
| `date` | `16 de abril de 2026` | `16 avril 2026` |
| `readTime` | `12 min de leitura` | `12 min de lecture` |
| `tag` | `TypeScript` | `TypeScript` (inalterado) |

Data em francês: sem preposição, mês em minúscula, sem vírgula. Meses: `janvier, février, mars, avril, mai, juin, juillet, août, septembre, octobre, novembre, décembre`.

`blocks_fr` espelha `blocks` bloco a bloco, na mesma ordem e com os mesmos `type`. Regras:
- `type: "code"` → o campo `code` é copiado **sem tradução**; apenas comentários dentro do código podem ser traduzidos se já estiverem traduzidos em `blocks_jp`.
- `type: "image"` → `src`, `width` e `height` idênticos; só `alt` é traduzido.
- `type: "heading"` → traduzir `text`, preservar `level`.
- `type: "list"` → traduzir cada item, preservar `ordered`.
- `type: "paragraph"` → traduzir `text`.

- [ ] **Step 3: Verificar que os quatro zeraram**

```bash
pnpm vitest run lib/i18n-coverage.test.ts 2>&1 | grep -cE "case-study-vox-pet|case-study-xdrop|circuit-breaker|design-system"
```

Esperado: `0`.

- [ ] **Step 4: Verificar que os JSONs seguem válidos e as rotas renderizam**

```bash
pnpm build
```

Esperado: build verde. Um JSON malformado quebra `getAllPosts` e derruba o build.

- [ ] **Step 5: Commit**

```bash
git add data/posts/case-study-vox-pet.json data/posts/case-study-xdrop.json data/posts/circuit-breaker-nodejs.json data/posts/design-system-shadcn-tailwind.json
git commit -m "$(cat <<'EOF'
feat(i18n): traduz 4 posts para frances (1/3)

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 10: Traduzir posts 5–8

**Files:**
- Modify: `data/posts/docker-for-frontend-devs.json`, `data/posts/inbox-cursor-pagination-virtualization.json`, `data/posts/multi-tenant-architecture-postgresql.json`, `data/posts/rag-langchain-postgres-fullcycle.json`

- [ ] **Step 1: Confirmar os campos pendentes**

```bash
nvm use 24 && pnpm vitest run lib/i18n-coverage.test.ts 2>&1 | grep -E "docker-for-frontend|inbox-cursor|multi-tenant|rag-langchain"
```

Esperado: 24 linhas.

- [ ] **Step 2: Traduzir os quatro posts**

Para cada arquivo, adicionar os 6 campos espelhando a estrutura de `_jp`. Formatos dos campos curtos:

| Campo | `pt` | `fr` |
|---|---|---|
| `date` | `16 de abril de 2026` | `16 avril 2026` |
| `readTime` | `12 min de leitura` | `12 min de lecture` |
| `tag` | `TypeScript` | `TypeScript` (inalterado) |

Data em francês: sem preposição, mês em minúscula, sem vírgula. Meses: `janvier, février, mars, avril, mai, juin, juillet, août, septembre, octobre, novembre, décembre`.

`blocks_fr` espelha `blocks` bloco a bloco, na mesma ordem e com os mesmos `type`. Regras:
- `type: "code"` → o campo `code` é copiado **sem tradução**; apenas comentários dentro do código podem ser traduzidos se já estiverem traduzidos em `blocks_jp`.
- `type: "image"` → `src`, `width` e `height` idênticos; só `alt` é traduzido.
- `type: "heading"` → traduzir `text`, preservar `level`.
- `type: "list"` → traduzir cada item, preservar `ordered`.
- `type: "paragraph"` → traduzir `text`.

Atenção especial em `inbox-cursor-pagination-virtualization.json`: é o post de métricas do inbox. Todos os números (`~6,7 MB`, `~19 KB`, `~400x`, `20.000 conversas`) devem sair idênticos, com vírgula decimal e unidades em `MB`/`KB`.

- [ ] **Step 3: Verificar que os quatro zeraram**

```bash
pnpm vitest run lib/i18n-coverage.test.ts 2>&1 | grep -cE "docker-for-frontend|inbox-cursor|multi-tenant|rag-langchain"
```

Esperado: `0`.

- [ ] **Step 4: Verificar o build**

```bash
pnpm build
```

Esperado: build verde.

- [ ] **Step 5: Commit**

```bash
git add data/posts/docker-for-frontend-devs.json data/posts/inbox-cursor-pagination-virtualization.json data/posts/multi-tenant-architecture-postgresql.json data/posts/rag-langchain-postgres-fullcycle.json
git commit -m "$(cat <<'EOF'
feat(i18n): traduz 4 posts para frances (2/3)

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 11: Traduzir posts 9–11 e fechar o guard

**Files:**
- Modify: `data/posts/starting-typescript.json`, `data/posts/testing-payment-integrations.json`, `data/posts/webhook-architecture-payment-providers.json`

- [ ] **Step 1: Confirmar os campos pendentes**

```bash
nvm use 24 && pnpm vitest run lib/i18n-coverage.test.ts 2>&1 | grep -E "starting-typescript|testing-payment|webhook-architecture"
```

Esperado: 18 linhas (3 posts × 6 campos).

- [ ] **Step 2: Traduzir os três posts**

Para cada arquivo, adicionar os 6 campos espelhando a estrutura de `_jp`. Formatos dos campos curtos:

| Campo | `pt` | `fr` |
|---|---|---|
| `date` | `16 de abril de 2026` | `16 avril 2026` |
| `readTime` | `12 min de leitura` | `12 min de lecture` |
| `tag` | `TypeScript` | `TypeScript` (inalterado) |

Data em francês: sem preposição, mês em minúscula, sem vírgula. Meses: `janvier, février, mars, avril, mai, juin, juillet, août, septembre, octobre, novembre, décembre`.

`blocks_fr` espelha `blocks` bloco a bloco, na mesma ordem e com os mesmos `type`. Regras:
- `type: "code"` → o campo `code` é copiado **sem tradução**; apenas comentários dentro do código podem ser traduzidos se já estiverem traduzidos em `blocks_jp`.
- `type: "image"` → `src`, `width` e `height` idênticos; só `alt` é traduzido.
- `type: "heading"` → traduzir `text`, preservar `level`.
- `type: "list"` → traduzir cada item, preservar `ordered`.
- `type: "paragraph"` → traduzir `text`.

`testing-payment-integrations.json` e `webhook-architecture-payment-providers.json` são densos em código — a maior parte do volume são blocos `type: "code"`, que só são copiados.

- [ ] **Step 3: Verificar que o guard inteiro ficou VERDE**

```bash
pnpm vitest run lib/i18n-coverage.test.ts
```

Esperado: **3 passed**. Este é o marco central do plano — nenhum objeto de tradução e nenhum campo de post pendente em nenhum dos cinco locales.

- [ ] **Step 4: Rodar a suíte inteira**

```bash
pnpm test
```

Esperado: todos verdes.

- [ ] **Step 5: Commit**

```bash
git add data/posts/starting-typescript.json data/posts/testing-payment-integrations.json data/posts/webhook-architecture-payment-providers.json
git commit -m "$(cat <<'EOF'
feat(i18n): traduz 3 posts para frances (3/3) e fecha o guard

Guard inteiro verde: nenhum objeto de traducao e nenhum campo de post
pendente nos cinco locales.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 12: Documentação, portão final e PR

**Files:**
- Modify: `public/llms.txt:3`, `public/llms.txt:9`
- Modify: `README.md:14`, `README.md:38`
- Modify: `CLAUDE.md:24`

- [ ] **Step 1: Atualizar `public/llms.txt`**

Linha 3: `Disponível em quatro idiomas (PT-BR / EN / ES / JA)` → `Disponível em cinco idiomas (PT-BR / EN / ES / JA / FR)`.

Linha 9: `O idioma principal é Português (BR), com versões completas em Inglês, Espanhol e Japonês.` → `…em Inglês, Espanhol, Japonês e Francês.`

- [ ] **Step 2: Atualizar `README.md`**

Duas afirmações estão erradas hoje: o número de idiomas e o mecanismo de i18n (o roteamento virou path-based em `009e0f5`; não é mais cookie).

Linha 14: `- **Quadrilingual** — PT-BR, English, Spanish and Japanese (cookie-based i18n)` → `- **Five languages** — PT-BR, English, Spanish, Japanese and French (path-based i18n)`

Linha 38: `- **i18n:** PT-BR / EN / ES / JA (cookie-based, quadrilingual)` → `- **i18n:** PT-BR / EN / ES / JA / FR (path-based, five locales)`

- [ ] **Step 3: Atualizar `CLAUDE.md`**

Linha 24: `Next.js 16 App Router portfolio with trilingual support (PT-BR / EN / ES).` → `Next.js 16 App Router portfolio with five locales (PT-BR / EN / ES / JA / FR).`

Na seção "i18n System", atualizar a descrição do tipo `Locale` para `"pt" | "en" | "es" | "jp" | "fr"` e acrescentar, ao fim da seção:

```markdown
- Adding a locale: append it to `LOCALES` in `lib/i18n.ts` and to the `HTML_LANGS`/`OG_LOCALES` maps. Everything else (proxy, sitemap, hreflang, `generateStaticParams`) derives from `LOCALES`. `lib/i18n-coverage.test.ts` then fails until every translation object, dictionary and post covers the new locale.
```

- [ ] **Step 4: Portão de verificação completo**

```bash
nvm use 24
pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm aeo
```

Esperado: tudo verde, AEO ≥ 90/100.

- [ ] **Step 5: Conferência manual das rotas francesas**

```bash
pnpm dev
```

Abrir e procurar texto em português que tenha escapado por um caminho não coberto pelo guard:

`/fr` · `/fr/sobre` · `/fr/projetos` · `/fr/engenharia` · `/fr/uses` · `/fr/busca` · `/fr/projetos/x-drop` · `/fr/posts/starting-typescript`

Verificar também no HTML servido: `<html lang="fr-FR">`, `og:locale` = `fr_FR`, e que o toggle de idioma navega `/fr/sobre` → `/jp/sobre` sem duplicar prefixo.

- [ ] **Step 6: Commit**

```bash
git add public/llms.txt README.md CLAUDE.md
git commit -m "$(cat <<'EOF'
docs: cinco idiomas em llms.txt, README e CLAUDE.md

README dizia quadrilingue e i18n por cookie -- o roteamento virou
path-based em 009e0f5. CLAUDE.md dizia trilingue. Ambos corrigidos,
mais a receita de como adicionar o proximo idioma.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 7: Push e PR — confirmar com o usuário antes**

Este é o primeiro passo do plano que sai da máquina local. **Perguntar antes de executar.** Com autorização:

```bash
git push -u origin feat/i18n-french
gh pr create --base main --title "feat(i18n): frances (fr-FR) como quinto idioma" --body "$(cat <<'EOF'
Adiciona francês (fr-FR) com paridade total aos outros quatro idiomas.

## Guard de cobertura

O motivo desse PR ser grande e de uma vez só: `t()` usa `Partial<Record<Locale, string>>`, então adicionar um locale não gera erro de compilação — conteúdo não traduzido cai em português em silêncio. Foi o que gerou 4 commits de fix na adição do japonês.

`lib/i18n-coverage.test.ts` fecha isso com três testes, escritos por locale e não para o francês:

1. Todo object literal com chave `pt` cobre todo `LOCALES` (via AST do TypeScript) — 281 objetos
2. `getDictionary` retorna objeto distinto por locale
3. Todo post traz os 6 campos `_<locale>` — 11 posts × 5 locales

Passam a proteger `jp` e `es` retroativamente. O sexto idioma herda o guard.

## Estrutural

`LOCALES` vira a única fonte de verdade: `proxy.ts` importa em vez de duplicar a lista, e as cadeias de ternário `locale → lang/og` viram `Record<Locale, string>` exaustivo, que o `tsc` cobra. `localeToHtmlLang` caía em `pt-BR` para locale não previsto — o francês teria saído com `lang="pt-BR"` passando em todos os checks.

Também consertado: `data/experiences.test.ts` tinha a própria lista de locales hardcoded.

## Verificação

`pnpm lint`, `typecheck`, `test`, `build` e `aeo` verdes. Conferência manual em todas as rotas `/fr`.

Spec: `docs/superpowers/specs/2026-08-02-i18n-frances-design.md`

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

---

## Critérios de aceite (do spec)

1. `/fr` e todas as rotas sob ele respondem 200, sem redirect para `/pt` — Task 2 + 12
2. `<html lang="fr-FR">` e `og:locale=fr_FR` — Task 2, verificado na Task 12
3. Toggle lista 🇫🇷 Français e preserva a rota na troca — Task 2, verificado na Task 12
4. `sitemap.xml` com 19 rotas × 5 locales e `hreflang` fr-FR — automático via `LOCALES`, verificado na Task 12
5. `pnpm test` verde nos três testes do guard — Task 11
6. `proxy.ts` sem lista de locales própria — Task 2
7. Nenhum texto em português nas páginas francesas — Task 12 Step 5
8. `README.md` e `CLAUDE.md` corretos — Task 12
9. Todos os checks verdes, AEO ≥ 90/100 — Task 12 Step 4
